export interface Viewport {
  zoom: number;
  latitude: number;
  longitude: number;
}

export interface FormAddPointDialogType {
  name: string;
  description: string;
  latitude: string;
  longitude: string;
}
